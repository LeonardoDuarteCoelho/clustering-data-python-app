# clustering-data-python-app
This Python application, developed with Streamlit, allows users to upload a CSV file containing consumer characteristics. After processing the data, the application uses a clustering model to group these consumers and provides a new CSV file for download, indicating the group to which each consumer belongs.
